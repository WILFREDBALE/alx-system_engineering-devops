##
# To gain a comprehensive understanding of Nginx configuration files and unlock the full potential of Nginx, it is recommended to explore the following URLs:
# - Nginx Wiki: Getting Started - https://www.nginx.com/resources/wiki/start/
# - Nginx Wiki: Configuration Pitfalls - https://www.nginx.com/resources/wiki/start/topics/tutorials/config_pitfalls/
# - Debian Wiki: Nginx Directory Structure - https://wiki.debian.org/Nginx/DirectoryStructure
#
# In most cases, administrators will remove this file from the "sites-enabled/" directory and keep it as a reference in the "sites-available/" directory. It will continue to receive updates from the nginx packaging team.
#
# This file automatically loads configuration files provided by other applications like Drupal or Wordpress. These applications will be accessible via a path named after the respective package, such as "/drupal8".
#
# For more detailed examples, please refer to the directory "/usr/share/doc/nginx-doc/examples/".
##

# Default server configuration
#
server {
	listen 80 default_server;
	listen [::]:80 default_server;

	add_header X-Served-By $HOSTNAME;

	# SSL configuration
	#
	# listen 443 ssl default_server;
	# listen [::]:443 ssl default_server;
	#
	# Note: It's recommended to disable gzip for SSL traffic.
	# See: https://bugs.debian.org/773332
	#
	# Refer to the ssl_ciphers documentation for ensuring a secure configuration.
	# See: https://bugs.debian.org/765782
	#
	# The self-signed certificates generated by the ssl-cert package should not be used in a production server!
	#
	# include snippets/snakeoil.conf;

	error_page 404 /404.html;
	location = /404.html {
		root /var/www/html;
		internal;
	}
	root /var/www/html;

	# If you are using PHP, include index.php in the list
	index index.html index.htm index.nginx-debian.html;

	server_name _;
	rewrite ^\/redirect_me https://www.youtube.com/watch?v=QH2-TGUlwu4 permanent;

	location / {
		# Serve the request as a file first, then as a directory, and if neither exists, display a 404 error.
		try_files $uri $uri/ =404;
	}

	location /airbnb-onepage/ {
		include proxy_params;
		proxy_pass http://0.0.0.0:5000;
	}

	location ~ ^/airbnb-dynamic/number_odd_or_even/(\d+)$ {
		include proxy_params;
		proxy_pass http://0.0.0.0:5001/number_odd_or_even/$1;
	}

	# Pass PHP scripts to the FastCGI server
	#
	#location ~ \.php$ {
	#	include snippets/fastcgi-php.conf;
	#
	#	# With php-fpm (or other unix sockets):
	#	fastcgi_pass unix:/var/run/php/php7.4-fpm.sock;
	#	# With php-cgi (or other tcp sockets):
	#	fastcgi_pass 127.0.0.1:9000;
	#}

	# Deny access to .htaccess files if Apache's document root
	# overlaps with nginx's document root
	#
	#location ~ /\.ht {
	#	deny all;
	#}
}


# Virtual Host configuration for example.com
#
# You can move this configuration to a different file in "sites-available/" and create a symbolic link in "sites
